import { placeholder } from "./placeholder";

// Sanity schemas for Superflow page templates. Replace `placeholder` with
// real schemas (integrationPage, useCasePage, libraryPage, comparisonPage,
// featurePage) as they're built. The studio requires at least one type.
export const schemaTypes = [placeholder];
