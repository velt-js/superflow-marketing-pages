// The published Tally form is the questionnaire source of truth.
// This repo owns the landing page and report, not response collection.
// See app/state-of-agency-tools/README.md for the current mapping.

/** Published survey form. Set to "" to show the pre-launch fallback. */
export const TALLY_FORM_ID = "ODqdPK";

/** Shared by metadata, sitemap and links. */
export const SURVEY_PATH = "/state-of-agency-tools";

/** Noindex while report fixtures are illustrative. */
export const REPORT_PATH = "/state-of-agency-tools/report";
